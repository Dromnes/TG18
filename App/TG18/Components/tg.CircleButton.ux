<Circle HitTestMode="LocalBoundsAndChildren" ux:Class="tg.CircleButton" ux:Name="circleButton">
	<string ux:Property="Text" />
	<float4 ux:Property="TextColor" />
	<Font ux:Property="Font" />
	<int ux:Property="FontSize" />

	<Text Value="{ReadProperty circleButton.Text}" Alignment="Center" Font="{ReadProperty circleButton.Font}" FontSize="{ReadProperty circleButton.FontSize}" Color="{ReadProperty circleButton.TextColor}" />

	<Shadow Size="5" Angle="70" />

	<WhilePressed>
		<Scale Factor=".9" Duration=".15" />
	</WhilePressed>
</Circle>